import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    zIndex: 10,
    alignItems: "center",
  },
  canvas: {
    flex: 1,
    width: 64,
    height: 233,
    top: -52,
    position: "absolute",
    zIndex: 10,
  },
});
